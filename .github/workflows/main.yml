name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: cubetiq/starter-docker-actions@v0.4
      - name: Starter Docker Test
        with:
          shell: bash
          run: docker version
