name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: cubetiq/starter-docker-actions@v0.3
      - name: Starter Docker Test
        run: |
          docker images
          docker ps
          docker --version
